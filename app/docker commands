# open terminal in docker 
docker-compose run [service_name] bash -> opens a new container
docker exec -ti CONTAINER_ID bash -> execute command in a running container

# execute commands in docker
docker-compose exec [service_name] [command]

# postgresql terminal inside container (run the command in container's terminal)
psql -U [username] -> it will ask password (username = postgres)

# exit container terminal
ctrl+p -> ctrl+q
